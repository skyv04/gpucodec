--- a/libavcodec/codec_id.h	2026-05-05 08:50:52.000000000 -0700
+++ b/libavcodec/codec_id.h
@@ -283,6 +283,12 @@
     AV_CODEC_ID_SVG,
     AV_CODEC_ID_GDV,
     AV_CODEC_ID_FITS,
+
+    /* GPU compute-shader intra codec, see libavcodec/agc1enc.c. Placed at
+     * the end of the video id list (after FITS, before the audio-id marker)
+     * so this codec occupies a stable enum slot outside upstream's own
+     * numbering and future upstream additions don't collide with it. */
+    AV_CODEC_ID_AGC1,
     AV_CODEC_ID_IMM4,
     AV_CODEC_ID_PROSUMER,
     AV_CODEC_ID_MWSC,
--- a/libavcodec/codec_desc.c	2026-05-05 08:50:52.000000000 -0700
+++ b/libavcodec/codec_desc.c
@@ -1689,6 +1689,13 @@
         .props     = AV_CODEC_PROP_INTRA_ONLY | AV_CODEC_PROP_LOSSLESS,
     },
     {
+        .id        = AV_CODEC_ID_AGC1,
+        .type      = AVMEDIA_TYPE_VIDEO,
+        .name      = "agc1",
+        .long_name = NULL_IF_CONFIG_SMALL("AGC-1 GPU compute-shader intra codec"),
+        .props     = AV_CODEC_PROP_INTRA_ONLY | AV_CODEC_PROP_LOSSY,
+    },
+    {
         .id        = AV_CODEC_ID_IMM4,
         .type      = AVMEDIA_TYPE_VIDEO,
         .name      = "imm4",
--- a/libavcodec/allcodecs.c	2026-05-05 08:50:52.000000000 -0700
+++ b/libavcodec/allcodecs.c
@@ -341,6 +341,8 @@
 extern const FFCodec ff_ulti_decoder;
 extern const FFCodec ff_utvideo_encoder;
 extern const FFCodec ff_utvideo_decoder;
+extern const FFCodec ff_agc1_encoder;
+extern const FFCodec ff_agc1_decoder;
 extern const FFCodec ff_v210_encoder;
 extern const FFCodec ff_v210_decoder;
 extern const FFCodec ff_v210x_decoder;
--- a/libavcodec/Makefile	2026-05-05 08:50:52.000000000 -0700
+++ b/libavcodec/Makefile
@@ -704,6 +704,8 @@
 OBJS-$(CONFIG_ULTI_DECODER)            += ulti.o
 OBJS-$(CONFIG_UTVIDEO_DECODER)         += utvideodec.o utvideodsp.o
 OBJS-$(CONFIG_UTVIDEO_ENCODER)         += utvideoenc.o
+OBJS-$(CONFIG_AGC1_DECODER)            += agc1dec.o
+OBJS-$(CONFIG_AGC1_ENCODER)            += agc1enc.o
 OBJS-$(CONFIG_V210_DECODER)            += v210dec.o
 OBJS-$(CONFIG_V210_ENCODER)            += v210enc.o
 OBJS-$(CONFIG_V210X_DECODER)           += v210x.o
--- a/libavformat/riff.c	2026-05-05 08:50:52.000000000 -0700
+++ b/libavformat/riff.c
@@ -417,6 +417,7 @@
     { AV_CODEC_ID_UTVIDEO,      MKTAG('U', 'M', 'H', '4') },
     { AV_CODEC_ID_UTVIDEO,      MKTAG('U', 'M', 'R', 'A') },
     { AV_CODEC_ID_UTVIDEO,      MKTAG('U', 'M', 'R', 'G') },
+    { AV_CODEC_ID_AGC1,         MKTAG('A', 'G', 'C', '1') },
     { AV_CODEC_ID_VBLE,         MKTAG('V', 'B', 'L', 'E') },
     { AV_CODEC_ID_ESCAPE130,    MKTAG('E', '1', '3', '0') },
     { AV_CODEC_ID_DXTORY,       MKTAG('x', 't', 'o', 'r') },
