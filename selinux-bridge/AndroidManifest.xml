<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.selinuxbridge.app">

    <!-- Without an explicit targetSdkVersion, aapt2 defaults to a very old
         API level, which trips Play Protect's "Unsafe app blocked: built
         for an older version of Android" warning on install. Declaring a
         current target here (matching the android-34 platform jar this is
         built against) avoids that. -->
    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="34" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Protocol v6 capture modes. Both of these are *runtime* permissions:
         declaring them here only makes them requestable, and MainActivity
         must still ask the user. A capture session that starts without the
         grant fails at open() time, not at build time, so the service
         checks the grant itself and returns a real error rather than
         handing back an empty stream. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Android 14+ requires the matching FGS type permission for a
         foreground service that touches the camera or the microphone. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- The bridge is useful on a device with no camera at all (it started
         life as a codec bridge), so nothing here may be load-bearing. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <!-- Gap #4 mitigations: bring the bridge back after a reboot or an
         in-place update, and let the user exempt it from Doze so a long
         idle period can't quietly stop it. Neither makes the service a
         real daemon (a force-stop still needs a manual launch), but
         together they remove the two cases that are recoverable. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false">

        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- "camera" and "microphone" are additive: without them in this
             list, startForeground() throws the moment a v6 capture session
             opens, even though the runtime permission was granted. -->
        <service android:name=".BridgeService"
            android:exported="false"
            android:foregroundServiceType="specialUse|camera|microphone" />

        <receiver android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- Some vendor ROMs (including Samsung's) send this instead
                     of, or ahead of, BOOT_COMPLETED on a quick restart. -->
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
